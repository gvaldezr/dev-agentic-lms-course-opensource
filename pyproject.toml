[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "moodle-course-pipeline"
version = "0.1.0"
description = "Automatizacion de paquetes de cursos Moodle desde DOCX"
readme = "README.md"
requires-python = ">=3.10"
